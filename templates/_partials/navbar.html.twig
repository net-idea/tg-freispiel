<nav class="navbar navbar-expand-lg navbar-dark glass-navbar">
  <div class="container">
    <a class="navbar-brand d-flex align-items-center" href="{{ path('app_index') }}" style="color: var(--theater-gold); font-weight: bold; font-size: 1.3rem;">
      <span style="font-size: 1.8rem; margin-right: 0.5rem;">🎭</span>
      Theatergruppe Freispiel
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item"><a class="nav-link {% if app.request is defined and app.request and app.request.attributes.get('_route') == 'app_index' %}active{% endif %}" href="{{ path('app_index') }}" style="color: var(--theater-text);">Home</a></li>
        <li class="nav-item"><a class="nav-link {% if app.request is defined and app.request and app.request.attributes.get('_route') == 'app_contact' %}active{% endif %}" href="{{ path('app_contact') }}" style="color: var(--theater-text);">Kontakt</a></li>
      </ul>
    </div>
  </div>
</nav>
