# In all environments, the following files are loaded if they exist,
# the latter taking precedence over the former:
#
#  * .env                contains default values for the environment variables needed by the app
#  * .env.local          uncommitted file with local overrides
#  * .env.$APP_ENV       committed environment-specific defaults
#  * .env.$APP_ENV.local uncommitted environment-specific overrides
#
# Real environment variables win over .env files.
#
# DO NOT DEFINE PRODUCTION SECRETS IN THIS FILE NOR IN ANY OTHER COMMITTED FILES.
# https://symfony.com/doc/current/configuration/secrets.html
#
# Run "composer dump-env prod" to compile .env files for production use (requires symfony/flex >=1.2).
# https://symfony.com/doc/current/best_practices.html#use-environment-variables-for-infrastructure-configuration

###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=change-this-to-a-random-secret
###< symfony/framework-bundle ###

## Database specific settings (Sqlite, MariaDB etc.)
DB=mysql
DB_HOST=localhost
DB_PORT=3306
DB_ROOT_PASSWORD=nopassword
DB_NAME=tg-freispiel
DB_USER=tg_freispiel
DB_PASSWORD=nopassword
DB_DSN="${DB}://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?charset=utf8mb4"

###> doctrine/doctrine-bundle ###
# For local development with Docker
#DATABASE_URL="mysql://app:!ChangeMe!@database:3306/app?serverVersion=mariadb-11.5.0&charset=utf8mb4"
# For local development without Docker
#DATABASE_URL="mysql://username:password@127.0.0.1:3306/database_name?serverVersion=mariadb-11.5.0&charset=utf8mb4"
DATABASE_URL="${DB}://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?serverVersion=10.11.2-MariaDB&charset=utf8mb4"
###< doctrine/doctrine-bundle ###

###> symfony/messenger ###
# Use the sync transport for Messenger to avoid DB-backed queue
MESSENGER_TRANSPORT_DSN=sync://
#MESSENGER_TRANSPORT_DSN=amqp://guest:guest@localhost:5672/%2f/messages
#MESSENGER_TRANSPORT_DSN=redis://localhost:6379/messages
#MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=0
###< symfony/messenger ###

###> symfony/mailer ###
# SMTP config split into parts; override these in .env.local for real credentials.
MAIL_SCHEME=smtp
MAIL_HOST=mail.domain.tld
# tls | ssl | (leave empty for none)
MAIL_ENCRYPTION=ssl
#MAIL_ENCRYPTION=tls
# Port for TLS: 587, for SSL: 465, without encryption: 25
MAIL_PORT=465
#MAIL_PORT=587

# URL-encoded credentials (do NOT use these as From headers)
# '@' => %40, '#' => %23
MAIL_USER=user@domain.tld
MAIL_PASSWORD=

# Composed DSN for Symfony Mailer
#MAILER_DSN=null://null
MAILER_DSN="${MAIL_SCHEME}://${MAIL_USER}:${MAIL_PASSWORD}@${MAIL_HOST}:${MAIL_PORT}?encryption=${MAIL_ENCRYPTION}&auth_mode=plain"
###< symfony/mailer ###
