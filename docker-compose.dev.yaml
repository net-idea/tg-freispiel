services:
  php:
    volumes:
      - .:/var/www/html
      - ./var:/var/www/html/var
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    environment:
      APP_ENV: dev
      APP_DEBUG: 1
      DATABASE_URL: '${DATABASE_URL:-sqlite:///%kernel.project_dir%/var/data_dev.db}'
    networks:
      - internal

  nginx:
    volumes:
      - .:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - internal
      - external

  node:
    image: node:24-alpine3.24
    container_name: '${APP_NAME:-tg-freispiel}_node'
    hostname: node
    working_dir: /app
    volumes:
      - .:/app
    command: >
      sh -lc "corepack enable &&
      corepack prepare yarn@1.22.22 --activate &&
      yarn install --frozen-lockfile &&
      yarn encore dev-server --host 0.0.0.0 --port 8080 --hot --public http://127.0.0.1:8080"
    ports:
      - '${NODE_PORT:-8080}:8080'
    networks:
      - internal

  mailer:
    image: axllent/mailpit:latest
    container_name: '${APP_NAME:-tg-freispiel}_mailer'
    hostname: mailer
    restart: unless-stopped
    ports:
      - '${MAILER_SMTP_PORT:-1025}:1025'
      - '${MAILER_WEB_PORT:-8025}:8025'
    environment:
      MP_SMTP_AUTH_ACCEPT_ANY: 1
      MP_SMTP_AUTH_ALLOW_INSECURE: 1
    networks:
      - internal
      - external

networks:
  internal:
  external:
    driver: bridge
